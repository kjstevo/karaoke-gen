[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "onnxruntime"
version = "999.0.0"
description = "Stub that redirects onnxruntime to onnxruntime-gpu"
requires-python = ">=3.10"
dependencies = ["onnxruntime-gpu"]
